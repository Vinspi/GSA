import { DigitOnlyDecDirective } from './digit-only-dec.directive';

describe('DigitOnlyDecDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitOnlyDecDirective();
    expect(directive).toBeTruthy();
  });
});
